#!/bin/sh

old_wd=$(pwd)

dir=$(dirname "$0")/..
cd "$dir"

inFile=meta/Config/config.dist.json
outFile=output/Meta.Config.Config/config.json

cp $inFile $outFile
envsubst < $inFile > $outFile

echo "Done"    

cd "$old_wd"