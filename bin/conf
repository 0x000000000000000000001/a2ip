#!/bin/sh

old_wd=$(pwd)

dir=$(dirname "$0")/..
cd "$dir"

inFile=conf/Config.dist.json
outFile=output/Config.Config/config.json

cp $inFile $outFile
envsubst < $inFile > $outFile

echo "Done"    

cd "$old_wd"